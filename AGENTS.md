# Local LLM Translator 开发约定

## 站点适配边界

- 高频、高价值且 DOM 或交互明显特殊的站点, 例如 X 和 YouTube, 可以为了体验使用专用 site adapter.
- CMS、电商、图库和卡片列表等可按 DOM 契约归类的页面, 优先使用通用的结构化块规则, 不要为每个域名增加一套专用分支.
- 不要在核心翻译流程里持续堆叠 `isSomeSite()` 判断. 站点特例应当集中在 adapter 或声明式规则中.

## 结构化块规则

- 结构化块用于“主文本 + 需保留的元数据”场景, 例如标题 + 价格、片名 + 番号、文章名 + 日期.
- 规则只描述稳定的 DOM 契约, 例如块选择器、主文本容器和可选的布局刷新条件. 文本提取、结果渲染、页面恢复和布局刷新由通用流程完成.
- 送给模型的文本必须与最终被替换的文本节点对应. 不要翻译整张卡片后只替换标题, 否则会重复或改写价格、日期、番号和标签.
- replace 模式应保留非翻译 DOM 结构和元数据. bilingual 模式应保留原卡片并单独显示译文.
- 卡片高度变化时, 优先使用标准浏览器事件或页面已有的布局机制. 避免注入可能被 CSP 拦截的内联脚本.

## 通用化原则

- 通用规则应当保守. 在无法稳定区分标题、价格、作者、日期或状态时, 宁可不处理该复合块, 也不要依赖激进的文本猜测.
- 只在真实页面暴露了现有契约无法表达的共同需求时, 才扩展规则接口. 不要为未出现的站点预先设计通用插件系统.
- 新增规则时至少验证: 主文本提取正确, 元数据只保留一份, replace 和 bilingual 模式正确, 恢复后 DOM 与布局正常.
